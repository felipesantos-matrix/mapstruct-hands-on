spring:
  profiles:
    active: dev
  application:
    name: matrix.aneel.query.api
  config:
    import: configserver:https://dev-app.matrixenergia.com/config-server-api
---
spring:
  cloud:
    config:
      name: matrix.aneel.query.api
      label: dev
  config:
    import: configserver:https://dev-app.matrixenergia.com/config-server-api
    activate:
      on-profile: dev
---

server:
  servlet:
    contextPath: /api
  port: ${PORT:8080}

spring.mvc.pathmatch.matching-strategy: ant-path-matcher
